### This file was generated by Nexus Schema
### Do not make changes to this file directly


type AuthPayload {
  token: String
  user: User
}

type Mutation {
  authenticate(cookie: String!): AuthPayload!
  deletePost(id: String!): ID!
  login(email: String!, password: String!): AuthPayload!
  post(description: String!, topic: String!, url: String!): Post!
  signup(email: String!, name: String!, password: String!): AuthPayload!
  vote(postId: String!): Vote
}

type Post {
  description: String
  id: String!
  postedBy: User
  topic: String!
  url: String!
  voters: [User!]!
}

type Query {
  feed: [Post!]!
  getTopicPosts(topic: String!): [Post!]!
  getUserId: String!
  getUserPosts(id: String): [Post!]!
}

type User {
  banReason: String
  banned: Boolean!
  email: String!
  id: String!
  name: String!
  posts: [Post!]!
  votes: [Post!]!
}

type Vote {
  post: Post!
  user: User!
}
